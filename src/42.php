<?php
// Define variables and constants
$variable1 = "value1";
$constant1 = 42;

// Perform some calculation or operations

// Example: Calculate area of a rectangle
$rectangle_area = $constant1 * $constant1;
echo "The area of the rectangle is: $rectangle_area";

// Example: Check if variable $variable1 exists
if (isset($variable1)) {
    echo "Variable $variable1 is set.";
} else {
    echo "Variable $variable1 is not set.";
}
?>
